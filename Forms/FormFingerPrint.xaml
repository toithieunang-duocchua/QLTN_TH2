<UserControl x:Class="QLTN.Forms.FormFingerPrint"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QLTN.Forms"
             mc:Ignorable="d" 
             Width="1000" Height="600">
    <UserControl.Resources>
        <!--  Bo góc cho TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="8"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Margin="0"
                                      x:Name="PART_ContentHost"
                                      VerticalScrollBarVisibility="Hidden"
                                      HorizontalScrollBarVisibility="Hidden"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#ddd"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"/>

                            <ContentPresenter Margin="10,0,35,0"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>

                            <ToggleButton Grid.Column="2"
                                      Focusable="false"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      HorizontalAlignment="Right"
                                      Width="35">
                                <Path Data="M 0 0 L 2 2 L 4 0 Z"
                                  Fill="#666"
                                  Stretch="Uniform"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center" Height="4" Width="6"/>
                            </ToggleButton>

                            <Popup Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide"
                                Width="150">
                                <Border CornerRadius="6"
                                    Background="White"
                                    BorderBrush="#ddd"
                                    BorderThickness="1">
                                    <ScrollViewer Margin="4,6"
                                              SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#5B63D0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="6"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="0"
                            Padding="15,8">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CenteredText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="AvatarStyle" TargetType="Border">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Background" Value="#5B63D0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="80"/>
            <!-- Row 0: Header -->
            <RowDefinition Height="Auto" MinHeight="70"/>
            <!-- Row 1: Search/Filter -->
            <RowDefinition Height="1*" MinHeight="200"/>
            <!-- Row 2: DataGrid -->
        </Grid.RowDefinitions>

        <!-- Header with gradient -->
        <Border Grid.Row="0"
                Background="#5B63D0"
                Padding="30,20">
            <StackPanel VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="👤 " FontSize="18"/>
                    <TextBlock Text="Quản lý Người thuê &amp; Vận hành"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="White"/>
                </StackPanel>
                <TextBlock Text="Số hộ thông tin người thuê và quản lý vận hành"
                           FontSize="13"
                           Foreground="#E8EAFF"
                           Margin="0,6,0,0"/>
            </StackPanel>
        </Border>

        <!-- Search/Filter Controls -->
        <Border Grid.Row="1"
                Background="White"
                Padding="30,15"
                BorderBrush="#E5E7EB"
                BorderThickness="0,1,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <Grid Grid.Column="0" Margin="0,0,15,0">
                    <TextBox x:Name="SearchBox"
                             Height="40"
                             FontSize="14"
                             Padding="12,10,40,10"
                             BorderBrush="#ddd"
                             BorderThickness="1"
                             Foreground="#333"
                             Background="White"
                             VerticalAlignment="Center"
                             Margin="0,0,193,0"/>

                    <TextBlock x:Name="Placeholder"
                               Text="Tìm kiếm theo tên, SDT,..."
                               Foreground="#999"
                               FontSize="14"
                               Padding="12,0,0,0"
                               IsHitTestVisible="False"
                               Visibility="Visible"
                               VerticalAlignment="Center"/>
                </Grid>

                <!-- Room Filter -->
                <ComboBox
                          Height="40"
                          SelectedIndex="0"
                          VerticalAlignment="Center"
                          Margin="555,0,15,0" Grid.ColumnSpan="2">
                    <ComboBoxItem Content="Tất cả phòng"/>
                    <ComboBoxItem Content="101"/>
                    <ComboBoxItem Content="102"/>
                    <ComboBoxItem Content="103"/>
                </ComboBox>

                <!-- Add Tenant Button -->
                <Button Grid.Column="2"
                        Content="➕ Thêm người thuê mới"
                        Width="Auto"
                        Height="40"
                        Background="#5B63D0"
                        Foreground="White"
                        FontSize="14"
                        VerticalAlignment="Center"
                        Padding="15,10"/>
            </Grid>
        </Border>

        <!-- DataGrid Container -->
        <Border Grid.Row="2"
                Background="#F9FAFB"
                Padding="20">
            <DataGrid x:Name="TenantsDataGrid"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeColumns="False"
                      IsReadOnly="True"
                      RowHeight="60"
                      GridLinesVisibility="None"
                      Background="White"
                      BorderThickness="0"
                      VerticalAlignment="Stretch"
                      ItemsSource="{Binding NguoiThueList}">

                <DataGrid.Resources>
                    <!-- Header Style -->
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="#666"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        <Setter Property="Padding" Value="15,12"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>

                    <!-- Row Style -->
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Margin" Value="0,0,0,0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>

                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F3F4FF"/>
                            </Trigger>

                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E8EAFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!-- Cell Style -->
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="10,0"
                                            Background="{TemplateBinding Background}"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.Resources>

                <!-- DataGrid Columns -->
                <DataGrid.Columns>

                    <!-- Column: NGƯỜI THUÊ with Avatar -->
                    <DataGridTemplateColumn Header="NGƯỜI THUÊ" Width="1.2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"
                                            Margin="10,0,0,0">
                                    <Border Width="36" Height="36" CornerRadius="18" 
                                            Background="#5B63D0" 
                                            Margin="0,0,12,0">
                                        <TextBlock Text="{Binding Avatar}"
                                                   Foreground="White"
                                                   FontWeight="Bold"
                                                   FontSize="16"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="{Binding Ten}"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="#1F2937"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Column: PHÒNG -->
                    <DataGridTextColumn Header="PHÒNG"
                                        Binding="{Binding Phong}"
                                        Width="0.8*"
                                        ElementStyle="{StaticResource CenteredText}"/>

                    <!-- Column: SỐ ĐIỆN THOẠI -->
                    <DataGridTextColumn Header="SỐ ĐIỆN THOẠI"
                                        Binding="{Binding SDT}"
                                        Width="1*"
                                        ElementStyle="{StaticResource CenteredText}"/>

                    <!-- Column: SỐ NGƯỜI Ở -->
                    <DataGridTextColumn Header="SỐ NGƯỜI Ở"
                                        Binding="{Binding SoNguoiO}"
                                        Width="0.8*"
                                        ElementStyle="{StaticResource CenteredText}"/>

                    <!-- Column: TRẠNG THÁI VÂN TAY -->
                    <DataGridTemplateColumn Header="TRẠNG THÁI VÂN TAY" Width="1.2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12"
                                        Padding="12,6"
                                        Background="{Binding MauTrangThai}"
                                        HorizontalAlignment="Center"
                                        MaxWidth="150">
                                    <TextBlock Text="{Binding TrangThai}"
                                               Foreground="White"
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Center"
                                               FontSize="12"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Column: HỢP ĐỒNG -->
                    <DataGridTemplateColumn Header="HỢP ĐỒNG" Width="1.2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12"
                                        Background="{Binding MauHopDong}"
                                        HorizontalAlignment="Center"
                                        Padding="12,6"
                                        MaxWidth="150">
                                    <TextBlock Text="{Binding HopDong}"
                                               Foreground="{Binding MauChuHopDong}"
                                               FontWeight="SemiBold"
                                               FontSize="12"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

    </Grid>
</UserControl>